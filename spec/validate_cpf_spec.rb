require 'spec_helper'

describe ValidateCpf do
  it 'has a version number' do
    expect(ValidateCpf::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
